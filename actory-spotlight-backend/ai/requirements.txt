# Python Dependencies for AI Emotion Analysis
# Python Version: 3.10.x (recommended: 3.10.13 for Render/TensorFlow compatibility)
# Install with: pip install -r requirements.txt

# NumPy for numerical operations (install first to avoid conflicts)
numpy==1.26.4

# TensorFlow/Keras for deep learning model
tensorflow==2.15.0

# OpenCV for video processing and face detection
opencv-python-headless==4.10.0.84

# Optional: GPU support (uncomment if using NVIDIA GPU)
# tensorflow-gpu>=2.13.0,<3.0.0
